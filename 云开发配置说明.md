# 云开发配置说明

## 1. 开通云开发

在微信开发者工具中导入本项目后：

1. 点击顶部“云开发”。
2. 创建一个云开发环境。
3. 复制环境 ID。
4. 打开 `miniprogram/app.js`，把这里改成你的环境 ID：

```js
cloudEnvId: '',
```

例如：

```js
cloudEnvId: 'your-env-id',
```

## 2. 部署登录云函数

在微信开发者工具左侧找到：

```text
cloudfunctions/login
```

右键选择：

```text
上传并部署：云端安装依赖
```

部署成功后，“我的”页面点登录，就可以拿到当前微信用户的 OpenID。

## 3. 创建数据库集合

在云开发控制台创建集合：

```text
parent_learning_states
```

建议权限先用：

```text
仅创建者可读写
```

这样每个家长只能读写自己的数据。

## 4. 当前降级逻辑

如果你还没有配置云开发环境 ID，小程序仍然可以运行，只是数据保存在手机本地缓存里。

配置云开发后，会优先使用云数据库同步。

# 云开发配置说明

## 快速开始

### 1. 开通云开发

在微信开发者工具中导入本项目后：

1. 点击顶部「云开发」按钮
2. 开通云开发，创建一个新的环境
3. 复制环境 ID（形如 `cloud1-xxx`）

### 2. 配置环境 ID

打开 `miniprogram/app.js`，找到以下代码：

```javascript
const cloudEnvId = 'your-cloud-env-id';
```

替换为你的环境 ID：

```javascript
const cloudEnvId = 'cloud1-xxx'; // 你的环境 ID
```

### 3. 创建数据库集合

在云开发控制台的「数据库」页面，创建以下集合：

```
parent_learning_states
```

权限设置为「所有用户可读，仅创建者可读写」。

### 4. 部署云函数

在开发者工具中：

1. 右键点击 `cloudfunctions/login` 目录
2. 选择「上传并部署：云端安装依赖」
3. 右键点击 `cloudfunctions/sync` 目录
4. 选择「上传并部署：云端安装依赖」

### 5. 测试同步

1. 在小程序中点击「我的」→「设置」
2. 找到「数据同步」区域
3. 点击「同步到云端」按钮
4. 显示「已同步」即配置成功

## 功能说明

### 自动同步

配置云开发后，每次数据变更会自动尝试同步到云端。

### 手动同步

在设置页面可以手动触发同步，适合以下场景：
- 网络不稳定时
- 首次配置后
- 数据冲突时

### 数据导出/导入

在设置页面的「数据管理」区域：
- **导出**：将数据复制到剪贴板，可粘贴到备忘录保存
- **导入**：从剪贴板读取数据（会覆盖当前数据）

## 多设备同步

1. 在设备 A 上配置并同步数据
2. 在设备 B 上使用同一微信号登录
3. 点击「同步到云端」即可获取设备 A 的数据

## 常见问题

### Q: 同步失败怎么办？

A: 检查以下几点：
1. 云开发环境 ID 是否正确
2. 云函数是否已部署
3. 数据库集合是否已创建
4. 网络是否正常

### Q: 数据会丢失吗？

A: 数据同时保存在本地和云端：
- 本地数据在微信存储中
- 云端数据在云开发数据库中
- 即使更换设备，云端数据也不会丢失

### Q: 如何备份数据？

A: 使用「导出数据」功能，将数据复制到备忘录或其他地方保存。

## 技术架构

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   小程序     │ ──► │   云函数     │ ──► │   数据库     │
│  (本地存储)  │ ◄── │   (sync)    │ ◄── │  (云端存储)  │
└─────────────┘     └─────────────┘     └─────────────┘
```

- **本地存储**：微信 Storage，快速读写
- **云函数**：处理数据同步逻辑
- **云端存储**：云开发数据库，持久化存储

## 未配置云开发

如果你还没有配置云开发环境 ID，小程序仍然可以正常运行，数据保存在手机本地缓存里。

配置云开发后，会自动启用云端同步功能。
